# Same dev placeholder already committed in docker/docker-compose.yml - see the note
# in db-credentials-secret.yaml about why that's fine for this non-production demo.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: tattoo-supply-manager
type: Opaque
stringData:
  JWT_SECRET: local-dev-jwt-secret-troque-em-producao-32chars+
